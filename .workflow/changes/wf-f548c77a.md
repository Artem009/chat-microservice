# [wf-f548c77a] Add Swagger documentation setup

## Product Context
<!-- PIN: product-context -->
**Product**: chat-microservice-2
**Type**: Backend API microservice

---

## User Story
**As a** developer
**I want** Swagger API documentation at /api-docs
**So that** I can test and document API endpoints interactively

## Description
Install @nestjs/swagger. Configure DocumentBuilder in main.ts with project title, description, version, and API key security (authorization header). Mount SwaggerModule at /api-docs with persistAuthorization enabled. Reference: comment-microservice/src/main.ts

## Acceptance Criteria

### Scenario 1: Swagger UI accessible
**Given** the app is running
**When** navigating to http://localhost:3000/api-docs
**Then** Swagger UI loads with "Chat microservice" title
**And** authorization security scheme is available

### Scenario 2: Endpoints documented
**Given** Swagger is configured
**When** a controller has @ApiTags decorator
**Then** endpoints appear grouped by tag in Swagger UI

## Technical Notes
- Install: `@nestjs/swagger`
- Add after Fastify adapter setup in main.ts
- Use `@fastify/static` for serving Swagger assets (already installed with Fastify story)

## Test Strategy
- [ ] E2E: GET /api-docs returns 200

## Dependencies
- wf-cfbf29f1 (Fastify adapter — Swagger setup comes after)

## Complexity
Low - Configuration in main.ts + package install
